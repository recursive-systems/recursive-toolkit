The skill bodies are the approved website prompts. Preserve their meaning when packaging. Edit skills here, then generate website copies using its tools:sync command. Do not add hooks, connectors, or execution scripts without a concrete workflow need.

Ship changes through pull requests to the default branch; do not push directly to it. For plugin releases, include a version bump in plugin.json and a changelog entry in the same pull request so marketplace sync can detect the release.
