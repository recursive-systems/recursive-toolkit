# Recursive Systems branding

`icon.png` and its vector source `icon.svg` are copied from the Recursive Systems website (`public/icon-512.png` and `public/icon.svg`). The 512 × 512 image is used for both the plugin logo and composer icon. Update both assets together when the brand changes.
